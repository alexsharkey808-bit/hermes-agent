name: web-trafilatura
version: 1.0.0
description: "Free in-process page reader (trafilatura + httpx) — extracts clean article text, no API key."
author: Hermes Agent
kind: backend
provides_web_providers:
  - trafilatura
